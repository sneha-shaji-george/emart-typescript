import { StyleLoadingSpinner, StyleMain } from "./style";

export default function LoadingSpinner() {

    return (
        <StyleMain>
            <StyleLoadingSpinner />
        </StyleMain>
    );
}